/**
 * DB-GPT DAG（有向无环图）包
 *
 * 提供 DAG 的核心实现，包括节点定义、边定义、图构建、
 * 拓扑排序等功能，支持复杂的工作流编排。
 *
 * <p>主要功能：</p>
 * <ul>
 *   <li>DAG - DAG 接口定义</li>
 *   <li>Node - 节点定义</li>
 *   <li>Edge - 边定义</li>
 *   <li>GraphBuilder - 图构建器</li>
 *   <li>TopologicalSorter - 拓扑排序器</li>
 * </ul>
 *
 * @since 1.0
 */
package com.dbgpt.workflow.dag;
