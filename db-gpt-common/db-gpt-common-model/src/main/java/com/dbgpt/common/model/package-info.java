/**
 * DB-GPT 通用数据模型包
 * 
 * 定义系统中使用的各种数据传输对象(DTO)、值对象(VO)、基础实体和领域对象，
 * 为各层之间的数据传输提供统一的数据结构，确保数据的一致性和可维护性。
 * 
 * <p>主要组成部分：</p>
 * <ul>
 *   <li>DTO - 数据传输对象，用于不同层级间的数据传输</li>
 *   <li>VO - 值对象，用于视图层的数据展示</li>
 *   <li>Base - 基础实体类，包含通用字段如ID、创建时间等</li>
 *   <li>Domain - 领域对象，表示业务领域的核心概念</li>
 * </ul>
 * 
 * @since 1.0
 */
package com.dbgpt.common.model;